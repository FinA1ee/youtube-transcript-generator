# YouTube 字幕生成与结构化总结实现说明

## 1. 如何获取并处理 YouTube 字幕

项目采用“先获取字幕，再生成报告”的两阶段流程。前端只提交 YouTube 视频 URL，后端负责解析和规范化输入，避免把第三方服务细节暴露给浏览器。

处理流程如下：

1. 前端调用 `POST /api/transcripts/fetch`，提交 YouTube URL。
2. 后端使用 URL 解析逻辑校验视频链接并提取标准化地址。
3. 后端通过 TranscriptAPI 获取字幕内容，API Key 只从 Worker 环境变量或密钥中读取。
4. 字幕数据被映射为统一的 `TranscriptSegment` 结构，包括开始时间、结束时间、文本、语言、字幕类型和可选说话人。
5. 后端执行长度、分段数量、字符数、视频时长等限制检查，防止过大的字幕进入 LLM 流程。
6. 成功后，后端把字幕封装进带过期时间和签名的 transcript token，返回给前端。

这样做的关键点是：前端不会直接接触 TranscriptAPI，也不会保存原始字幕；后续报告生成只依赖后端签发的 transcript token。

## 2. 如何调用 Gemini 并实现流式输出

报告生成通过 `POST /api/reports/stream` 启动。前端提交 transcript token，后端验证 token 后恢复字幕上下文，然后调用 Gemini。

Gemini 输出采用 NDJSON 协议：模型每次输出一行完整 JSON，后端增量解析并转换成类型化事件，例如：

- `title`：报告标题和副标题。
- `heading`：层级标题，支持 1、2、3 级。
- `paragraph`：归属于某个标题的总结段落。

后端把这些事件通过 SSE 风格的流式响应发送给浏览器。前端逐帧读取响应 body，解析 `data:` 内容，并按事件类型更新页面。标题、章节和段落会通过打字机效果逐步渲染；如果用户点击跳过动画，则立即展示已排队内容。

这种设计的好处是用户不必等待完整报告生成完成，可以边生成边阅读；同时，后端仍然保留对 Gemini 输出格式、错误清洗和结构校验的控制。

## 3. 如何根据用户生成需求影响输出结果

页面支持可选的自然语言生成需求输入。用户可以描述任务类型、输出风格、目标读者和约束，例如“面向初学者，生成简洁学习笔记”。

实现上，这个字段只在报告生成阶段使用：

1. 字幕获取阶段不接收、不处理生成需求。
2. 前端启动报告流时，把非空的 `generationRequirements` 与 transcript token 一起提交。
3. 后端对该字段进行修剪、类型校验和长度限制。
4. Gemini prompt 中会加入“用户生成需求”段落，但明确说明它只能作为有限指导。

用户需求不能覆盖系统级约束，例如：

- 输出必须是简体中文。
- Gemini 必须返回 NDJSON。
- 不能输出原始字幕全文。
- 不能改变事件结构。
- 不能暴露密钥、供应商错误或内部实现细节。

因此，用户需求会影响内容倾向，但不会破坏应用的固定输出协议。

## 4. 如何实现章节级 5W1H 总结

主报告使用层级标题组织内容，其中 1 级标题被视为“章节”。章节级 5W1H 功能基于这个结构实现。

报告流启动后，后端会创建一个服务端报告上下文，并向前端发送一个 `report_context` 事件。这个事件只包含 `reportContextId`，不包含字幕、全文报告或敏感上下文。

后端保存的上下文包括：

- 已验证的字幕内容。
- 用户生成需求。
- 流式生成过程中累计的标题、章节和段落。
- 可用于定位章节的层级结构。

当用户点击章节标题旁边的 `[5W1H]` 按钮时，前端只提交：

- `reportContextId`
- `chapterId`

前端不会重新提交整篇文章、原始字幕或所有段落。后端通过 `reportContextId` 查找服务端保存的上下文，再根据 `chapterId` 找到对应章节及其子标题、段落，同时结合完整视频字幕上下文调用 Gemini。

5W1H 返回固定结构：

- Who
- What
- When
- Where
- Why
- How

如果上下文不足以回答某一项，要求模型使用稳定的“未提及”，避免编造信息。前端以固定六行格式渲染结果，保持展示一致性。

## 5. 关键项目取舍与亮点

### 取舍

- 使用服务端内存保存报告上下文，而不是让前端回传全文。这样更符合安全和数据边界要求，但上下文会受 Worker 生命周期和 TTL 限制影响。
- 使用 NDJSON 而不是一次性 JSON。这样支持真正的流式渲染，但要求后端实现增量解析和错误处理。
- 使用自然语言生成需求，而不是多个结构化表单字段。这样用户表达更灵活，但需要通过 prompt 明确限制它的影响范围。
- 以 1 级标题作为章节边界，而不是新增 chapter 事件。这样复用现有层级标题模型，减少协议复杂度。

### 亮点

- 字幕获取、报告生成、Gemini 诊断和章节 5W1H 请求被拆成独立接口，职责清晰。
- API Key、TranscriptAPI Key、token secret 等敏感信息只存在于服务端。
- Gemini 输出经过结构化协议约束，前端不直接渲染不可控大段文本。
- 报告支持渐进式显示、取消、重试、跳过动画和部分内容保留。
- 章节 5W1H 使用服务端上下文完成，避免前端重复上传全文，降低数据泄露和载荷膨胀风险。
- 测试覆盖了字幕处理、Gemini prompt、流式输出、上下文保存、5W1H 请求校验和前端资源行为。
